<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.activeacademy.portal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:supportsRtl">

        <activity android:name="org.activeacademy.portal.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.activeacademy.portal.LoginActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="org.activeacademy.portal.HomeActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="org.activeacademy.portal.FeedbackActivity"
            android:screenOrientation="portrait" />

        <!-- This is required if you want to do any message handling beyond receiving notifications
         on apps in the background. -->
        <service android:name="org.activeacademy.portal.utils.MessagingService">
            <intent-filter>
                <action android:name="org.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>